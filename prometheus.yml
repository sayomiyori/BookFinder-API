global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: bookfinder-api
    static_configs:
      - targets:
          - host.docker.internal:8001  # API порт из docker-compose.yml
    metrics_path: /metrics
